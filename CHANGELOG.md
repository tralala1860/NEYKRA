# CHANGELOG — NEYKRA

> À mettre à jour à la fin de chaque session de développement (Claude Code, Cline ou autre).
> Format : date, ce qui a été fait, ce qui reste à faire / bugs connus.

## [15/09/2026] — Session 8 — Correctif critique (base de données) : trigger handle_new_user_auth obsolète

- **Correctif critique (base de données) : trigger handle_new_user_auth obsolète** — le trigger réellement actif dans Supabase était resté sur sa version Phase 0 (insert minimal, aucune lecture de `raw_user_meta_data`), jamais resynchronisé après son enrichissement en Phase 1 (Session 2). Conséquence : tout compte créé avec confirmation d'email activée recevait un username générique (`user_<uuid>`) et un birthdate NULL, quels que soient les champs saisis à l'inscription. Corrigé en réexécutant manuellement `compute_is_minor` + `handle_new_user_auth` + le trigger (section 5 de `supabase/schema.sql`) dans le SQL Editor Supabase ; vérifié via `select prosrc from pg_proc where proname = 'handle_new_user_auth'` que la version active contient bien `raw_user_meta_data` et `on conflict (id) do update`. Le compte de Binate (créé avant le correctif) a été réparé manuellement (username/display_name = `neykra`). **Point d'attention pour l'avenir** : toute modification de fonction/trigger dans `schema.sql` doit être réexécutée manuellement dans Supabase — rien n'est automatisé ni suivi par migration pour cette partie du schéma (contrairement aux fichiers `supabase/migrations/`). Si d'autres comptes de test ont été créés avant ce correctif, ils ont probablement le même problème et nécessiteraient la même réparation manuelle.
- **Posts affichés sur la page de profil** (ferme le manque principal de la Phase 2) — `src/lib/posts/actions.ts` : nouvelle Server Action `getPostsByAuthor(authorId)` (tri date décroissante, limite 30) ; la logique complète de `getFeedPosts` (join profiles + otaku_status, décompte des réactions par type, réaction de l'utilisateur courant, compteur + commentaires) est factorisée dans une fonction interne partagée `fetchVisiblePostsWithMeta` — comportement du fil principal inchangé (limite 50 conservée). Même visibilité RLS que le fil (`posts_select` : soi / ami accepté / public non bloqué). UI `src/app/profile/[username]/page.tsx` : section « Publications » sous les infos de profil, réutilisant `PostCard` tel quel (réactions, commentaires, style citation `.neykra-quote` pour les posts sans média, suppression si auteur) ; message « Aucune publication pour l'instant. » si aucun post ; compteur sobre (« N publications ») à côté du @username. Fil principal, réactions, suppression de commentaire, auth : intacts. Qualité : `npx tsc --noEmit` ✓ (0 erreur, y compris après `next typegen`), `next build` ✓ compilé (Turbopack ~11-18 s) — l'étape TypeScript du build dépasse la fenêtre d'exécution de 30 s de l'environnement, validée via `next typegen` + `tsc` (exit 0).



## [15/09/2026] — Session 10 — Suppression de ses propres commentaires (bouton branché sur deleteComment)

- **Bouton de suppression des commentaires** (ferme le manque « deleteComment jamais branché à l'UI » de la Phase 2) — `src/app/feed/comment-list.tsx` : chaque commentaire dont l'auteur est l'utilisateur connecté affiche un bouton « Supprimer » — `Button` `variant="ghost"` `size="sm"` + `className="text-[var(--color-error)] hover:bg-[var(--color-error)]/10"`, **exactement le pattern déjà utilisé pour la suppression de post dans `post-card.tsx`** — aligné à droite sous le contenu de la bulle. Aucun pattern de confirmation n'existe dans le projet (`confirm()`/`window.confirm` absents partout, dossier vérifié) → suppression directe, cohérente avec la suppression de post. Au clic : appel de la Server Action `deleteComment` dans un `useTransition` (pattern déjà utilisé pour les réactions dans `post-card.tsx`), libellé « Suppression… » + bouton désactivé pendant l'appel, disparition immédiate du commentaire **sans rechargement de page**, bandeau d'erreur `role="alert"` (style des bandeaux existants) si le serveur refuse.
- **Qui peut supprimer ? (RLS vérifiée)** — `supabase/schema.sql` §6.4 : `comments_delete_own … for delete using (author_id = auth.uid())`. **Seul l'auteur du commentaire** peut supprimer le sien ; l'auteur du post n'a **aucun** droit sur les commentaires d'autrui (aucune policy de modération) → le bouton n'apparaît donc **jamais** sur les commentaires des autres (comparaison `comment.author_id === currentUserId`).
- **`deleteComment` durcie, signature inchangée** (`(commentId: string) => Promise<DeleteCommentFormState>`) : garde `!commentId` ajoutée, et surtout `.select("id")` en fin de `delete()` pour détecter le cas **0 ligne supprimée**. C'était un vrai trou : PostgREST ne lève aucune erreur quand la RLS bloque la suppression ou que la ligne n'existe plus — l'ancienne version renvoyait donc `{ success: "Commentaire supprimé." }` et l'UI aurait retiré un commentaire toujours présent en base. Désormais : erreur explicite si `data.length === 0`, et `revalidatePath("/feed")` uniquement sur une suppression réellement effectuée. `getUser()` (auth) et le filtre `author_id = user.id` (défense en profondeur) sont conservés.
- **Prop `currentUserId` optionnelle** ajoutée sur `PostCard` et transmise à `CommentList` ; fournie par `src/app/feed/feed-list.tsx` (déjà en possession de `currentUserId`) et `src/app/profile/[username]/page.tsx` (`user?.id`). Aucune modification de la logique de pagination, de profil, de réaction ni de post.
- **NEYKRA_SPEC.md §9 (Phase 2)** : ligne « Manque : affichage des posts de l'utilisateur sur son propre profil » retirée (livré en `a2093d3`) et ligne « Manque : deleteComment … jamais branché à l'UI » retirée (livré ici) ; restent « édition de commentaire » et l'édition/préview de post.
- **Non touché** : posts, réactions, pagination (« Charger plus »), `/login`, authentification, schéma Supabase (aucune migration).
- **Qualité** : `npm run build` **✓ exit 0, build complet observé pour la première fois** (`✓ Compiled successfully in 6.9s`, **`Finished TypeScript in 9.3s`**, `✓ Generating static pages (9/9)`, table des routes : 8 routes + proxy middleware). Les deux premières tentatives de la session avaient été coupées par le timeout de 30 s de l'environnement pendant `Running TypeScript ...` (compilation OK : 10,4 s puis 10,2 s) — le cache Turbopack chaud a ensuite laissé passer l'étape complète. `npx eslint` sur les fichiers touchés : **0 erreur** ; une erreur `react/no-unescaped-entities` **préexistante** subsiste dans `src/app/profile/[username]/page.tsx` ligne 166 (apostrophe typographique de « l'instant », livrée en `a2093d3`) — vérifiée identique sur `HEAD` (via ESLint `--stdin` sur la version committée) et laissée telle quelle car hors périmètre. `npx next typegen` + `npx tsc --noEmit` ✓ exit 0.

## [15/09/2026] — Session 9 — Fil d'actualité : pagination « Charger plus » (fin de la limite dure de 50 posts)

- **Pagination par curseur sur `created_at` du fil `/feed`** (ferme le dernier manque de la Phase 2 côté fil) — `src/lib/posts/actions.ts` : la fonction interne partagée `fetchVisiblePostsWithMeta` accepte désormais un curseur optionnel `before` (filtre PostgREST `.lt("created_at", before)` — strictement avant la date) et renvoie `{ posts, hasMore, error? }` au lieu d'un tableau nu. `hasMore` est calculé en **sondant un post de plus** que la limite demandée (`limit + 1`) puis retiré de la page renvoyée : aucune requête supplémentaire, aucune fonction SQL, aucune migration. `getFeedPosts()` conserve le comportement historique exact (50 posts les plus récents) et renvoie maintenant `{ posts, hasMore }` (son unique consommateur est `src/app/feed/page.tsx`) ; nouvelle action `getMoreFeedPosts(before)` → 25 posts suivants + `hasMore`, entrée validée (`Date.parse` : curseur invalide → page vide silencieuse, car une Server Action reste appelable directement en POST). Tailles de page factorisées (`FEED_PAGE_SIZE = 50`, `FEED_MORE_PAGE_SIZE = 25`, `PROFILE_POSTS_LIMIT = 30`).
- **`getPostsByAuthor` non cassée** : elle consomme `.posts` du même interne et garde sa signature `Promise<PostWithAuthor[]>` (30 derniers posts, aucune pagination côté profil) → page de profil, compteur « N publications » et `PostCard` strictement intacts.
- **UI `/feed`** — nouveau composant client `src/app/feed/feed-list.tsx` : garde en état **uniquement les pages supplémentaires** (la page initiale reste pilotée par le serveur, donc un post fraîchement créé apparaît toujours en tête via le `revalidatePath("/feed")` de `createPost`), dédoublonnage par `id` (un post publié entre-temps peut figurer à la fois dans la page revalidée et dans une page supplémentaire), curseur = `created_at` du dernier post affiché, `useTransition` pour l'état de chargement. Bouton « Charger plus » (`Button` `variant="secondary"` — seul composant UI réutilisé, conforme à la règle permanente spec §6.10) affiché seulement si `hasMore`, spinner natif du `Button` (`loading={isPending}`) pendant l'appel, disparition automatique quand le serveur annonce la fin de la liste (aucun message superflu) ; en cas d'échec réseau/base, bandeau `role="alert"` et le bouton reste disponible pour réessayer. Aucun rechargement de page. `src/app/feed/page.tsx` ne fait plus que transmettre `posts` / `hasMore` / `userId` au composant ; l'état vide (« 0 / Aucune publication ») est inchangé.
- **Non touché** (chantiers séparés) : page de profil, réactions (6 réactions manga), commentaires et suppression de commentaire, `/login`, authentification, schéma Supabase (le curseur s'appuie sur la colonne `created_at` existante et sur la RLS `posts_select` inchangée). `NEYKRA_SPEC.md` §9 laisse encore la case « Manque : pagination » non cochée — à mettre à jour lors de la prochaine passe de roadmap.
- **Qualité** : `npx eslint` ✓ (0 erreur sur les 4 fichiers touchés), `npx tsc --noEmit` ✓ exit 0 **après `npx next typegen`**, `next build` ✓ **compilation réussie** (Turbopack : 13,5 s, puis 9,3 s au 2e essai) — comme en Session 8, l'étape `Running TypeScript ...` du build reste au-delà de la fenêtre d'exécution de 30 s de l'environnement et se fait **couper** (deux tentatives) : elle n'a donc **pas** été observée verte *dans le build*, et c'est la validation `next typegen` + `tsc --noEmit` qui fait foi. Vérifié au passage que `Date.parse` accepte bien les `created_at` renvoyés par PostgREST à 6 décimales (`2026-09-15T03:01:12.345678+00:00` comme en `Z`) tout en rejetant une chaîne invalide. Point de vigilance documenté : curseur mono-colonne `created_at`, deux posts partageant un `created_at` strictement identique à cheval sur une frontière de page pourraient être sautés (improbable avec `now()` par insertion) — un curseur composite `(created_at, id)` réglerait ce cas de bord si le besoin apparaît.

## [14/09/2026] — Session 7 — Identité « encre manga » appliquée à /login

- **Post texte seul mis en valeur façon citation** — `src/app/feed/post-card.tsx` : si le post n'a **aucun média** (`media_url` null), le texte est affiché centré dans un bloc `.neykra-quote` (nouvelle classe `globals.css`) ; s'il a un média, le texte garde son style actuel normal à gauche. `.neykra-quote` : dégradés radiaux doux d'encre en fond (`--neykra-accent-1` à 8 % / `--neykra-accent-2` à 6 % via `color-mix`, dans un `@supports`, zéro SVG/animation — ambiance, pas distraction), texte centré `var(--text-primary)` avec taille fluide `clamp(18px → 24px)` (une punchline ressort, un pavé reste lisible), `line-height: 1.6`, padding confortable, coins arrondis `rounded-lg` cohérents avec la Card. Que des tokens `--neykra-*`/`--text-primary` → 5 univers sans réglage. Réactions, commentaires, logique de création de post : intacts.
- **Qualité** : `npm run build` ✓ (compilation + TypeScript OK, 9/9 pages). À faire : commit (en attente).

- **Bulles de dialogue BD pour les commentaires (visuel uniquement, aucune logique touchée)** — `src/app/feed/comment-list.tsx` : chaque commentaire est désormais présenté façon BD classique — avatar de l'auteur à gauche (image, ou initiale sur fond `--accent-subtle` si pas d'avatar, même pattern que l'en-tête des posts), bulle à droite avec **pointe vers l'avatar** via `::before` (losange rotaté, `clip` sans HTML supplémentaire). Nouvelle classe CSS `.neykra-bubble` dans `globals.css` : cible `.neykra-bubble > .neykra-panel` (la Card garde ses coins arrondis `rounded-xl`, son fond `var(--surface)` et sa bordure 2px `var(--text-primary)` de `.neykra-panel` — la pointe s'aligne sur cette bordure réelle, pas sur `--border`), padding interne confortable (`0.75rem 1rem` ≈ py-3 px-4). Texte inchangé (`text-sm`, `var(--text-primary)`, `whitespace-pre-wrap`) — lisibilité prioritaire (spec 6.2). Que des tokens existants (`--surface`, `--text-primary`, `--border`, `--accent-subtle`, `--accent`) → fonctionnel sur les 5 univers sans ajustement. `comment-form.tsx` volontairement inchangé (champ de saisie, pas un message — une pointe sur un input serait une décoration trompeuse, interdit §6.3). Réactions, /login, auth : intacts.
- **Qualité** : `npm run build` ✓ (compilation + TypeScript OK, 9/9 pages). À faire : commit (en attente).

- **Réactions manga sur les posts (migration 005 — À EXÉCUTER dans le SQL Editor Supabase)** : `supabase/migrations/005_post_reactions.sql` (idempotente) — ajoute `likes.reaction_type text` (CHECK sur 'like'/'love'/'haha'/'wow'/'sad'/'fire', défaut 'like' → les likes existants deviennent des réactions 'like', aucune perte) **+ contrainte UNIQUE (post_id, user_id)** « une seule réaction par utilisateur par post » (dédoublonnage préalable des doublons éventuels, la plus ancienne réaction est conservée). `NEYKRA_SPEC.md` §4 mis à jour.
- **Server Actions `src/lib/posts/actions.ts`** : `toggleLike` remplacé par `toggleReaction(postId, reactionType)` (validation serveur du type ; pas encore réagi → insère / même type → supprime / type différent → remplace, modèle Facebook, appuyé par la contrainte UNIQUE). `getFeedPosts` retourne désormais `reactions` (décompte par type, ex. `{ like: 3, fire: 2 }`) et `user_reaction` (type de la réaction de l'utilisateur courant ou null) au lieu de `like_count`/`has_liked` — une seule requête `likes` pour tous les posts.
- **Types `src/lib/posts/types.ts`** : `REACTION_TYPES` (const), `ReactionType`, `ReactionCounts`, `ReactionToggleState` (remplacent `LikeToggleState`) ; `PostWithAuthor` avec `reactions` + `user_reaction`.
- **UI `src/app/feed/post-card.tsx`** : le bouton like devient un sélecteur de réactions — au survol (desktop, `group-hover`) ou appui long 400 ms (mobile, `onTouchStart`), une petite `Card` affiche les 6 emojis (👍 like, 💥 love, 😂 haha, 😱 wow, 😢 sad, 🔥 fire) au-dessus du bouton ; clic sur un emoji → `toggleReaction` ; bouton principal = emoji de la réaction active (variante primary + glow) ou icône like par défaut (clic = like par défaut / toggle off si déjà réagi) ; compteurs par type sous le bouton (ex. « 👍 3 · 🔥 2 »), uniquement les types ≥ 1. Composants Button/Card uniquement, tokens `--neykra-*`/`--text-*` (aucune couleur en dur).
- **Hors périmètre** : commentaires, /login, authentification — intacts (commentaires = étape séparée).
- **Qualité** : `npm run build` ✓ (compilation + TypeScript OK, 9/9 pages). À faire : exécuter 005_post_reactions.sql côté Supabase ; commit (en attente).

- **Page `/login` (`src/app/login/page.tsx`)** : même direction visuelle que le hero de l'accueil, sans rien réinventer — `<main>` reçoit `.neykra-hero` (trame de points halftone) + les 4 divs `.neykra-hero-lines--tl/tr/bl/br` (lignes de vitesse des coins). Le titre « Connexion » reprend le traitement exact du titre NEYKRA : éclaboussure d'encre SVG (tache accent-1 opacité 0.85 + contre-tache accent-2 + projections, mêmes paths), halo de lisibilité via la classe `.neykra-hero-title`, Anton uppercase — mais en plus petit (`text-5xl` au lieu de `text-9xl`, page secondaire) avec annotation « Chapitre 02 » façon numérotation. Sous-titre et bannière de confirmation email inchangés.
- **Formulaire** : encapsulé dans le composant `Card` existant (`src/components/ui/Card.tsx`, sobre, `variant="default"`, `padding="lg"`) — plus de conteneur nu. Le bouton de soumission reçoit `.neykra-btn-hero` (coins coupés + ombre décalée accent), même usage que la home.
- **Hiérarchie visuelle respectée (spec 6.2)** : les inputs email/mot de passe ne bougent pas d'un pixel (classes sobres, focus visible, aucune décoration) — lisibilité et fonctionnalité prioritaires.
- **Zéro couleur en dur** : tout passe par les tokens `--neykra-*` de l'univers actif → rendu automatique cohérent sur les 5 univers (VOID, NEON TOKYO, SAKURA, INFERNO, ZEN).
- **Aucune nouvelle classe CSS** créée dans `globals.css` ; aucune logique d'authentification ni autre page touchée.
- **Qualité** : `npm run build` ✓ (compilation + TypeScript OK, 9/9 pages).
- **Correction (même session)** : taches d'encre du hero (`/`) et de `/login` passées d'aplats unis (effet « autocollant ») à des dégradés SVG façon encre qui se dilue — `<radialGradient>` (`neykra-ink-main` : accent-1 opaque au cœur → accent-2 quasi transparent en périphérie, `gradientUnits="userSpaceOnUse"`) pour la tache principale et `<linearGradient>` (`neykra-ink-second` : lavis accent-2 → invisible) pour la contre-tache ; toujours 100 % piloté par les tokens `--neykra-accent-*` (aucune couleur en dur). Annotation « Chapitre 02 » retirée de `/login` — la numérotation de chapitre reste réservée à la home.
- **Raffinement (même session)** : halo des titres renforcé (`.neykra-hero-title` : stroke 5px + double drop-shadow doux en `var(--neykra-bg)`), deux traitements de titre distincts — `.neykra-title-brand` (home, NEYKRA) : lettrage pinceau via filtre SVG `#neykra-brush` (feTurbulence + feDisplacementMap) + text-shadows décalés irréguliers ; `.neykra-title-page` (`/login`) : contour encre net 6px + ombre décalée dure façon onomatopée (nouveau token `--neykra-ink`, constante manga commune aux 5 univers) ; numérotation « Chapitre 01 » supprimée de la home (aucune référence chapitre dans le code) ; tache d'encre redessinée (paths asymétriques à épaisseur variable + bavures fines partant du corps, gouttes groupées de tailles variées, même SVG sur home et login) ; lignes de vitesse `.neykra-hero-lines--*` rendues organiques (3 nappes désalignées : angles/espacements/épaisseurs différents, taille et opacité propres à chaque coin).
- **À faire** : commit de la session (en attente de validation visuelle) ; vérifier le rendu de la tache d'encre dégradée sur les univers clairs (SAKURA/ZEN).
- **Ajustement (même session)** : espacement du formulaire `/login` aéré — `gap-4` → `gap-6` sur le conteneur flex du `LoginForm` (champs, bouton et lien d'inscription uniformément espacés via une seule classe Tailwind, aucune marge ad hoc) ; `padding="lg"` de la Card conservé.
- **Ajustement (même session)** : `/login` recentré — `<main>` en `flex min-h-screen items-center justify-center` (Card centrée horizontalement et verticalement dans le viewport, décorations `.neykra-hero` + lignes de vitesse restant plein écran derrière) ; contenu du `LoginForm` centré (`items-center`), labels `text-center` au-dessus de leurs champs, inputs et bouton en `w-full max-w-sm`, padding interne des inputs confortable (`px-4 py-3`) pour que la saisie ne touche jamais les bords. Aucun autre élément touché.
- **Correctif (même session) : upload de médias sur /feed bloqué en 413** — `next.config.ts` ajoute `experimental.serverActions.bodySizeLimit: "10mb"` (Next.js limite le body des Server Actions à 1 Mo par défaut, ce qui bloquait tout upload avant même la validation applicative de `createPost`). La limite réelle perçue par l'utilisateur reste celle de `createPost` (5 Mo) ; celle de Next.js sert uniquement de marge pour ne pas interférer. Vérifié : `npm run build` ✓ (build + dev redémarré, `Experiments: serverActions` bien chargé dans les deux), publication d'un post avec image confirmée fonctionnelle en local par Binate.



- **Page d'accueil publique `src/app/page.tsx`** : refonte complète direction « encre manga » (noir/blanc/accent) — numérotation « Chapitre 01 » (tracking large, discret), titre NEYKRA en Anton très grand format avec éclaboussure d'encre SVG organique derrière (tache principale accent-1 opacité 0.85 + contre-tache accent-2 + projections), sous-titre sur bandeau surlignage irrégulier (clip-path polygonal façon marqueur, pas de bulle arrondie), texte explicatif sobre (var(--text-secondary)).
- **CSS `src/app/globals.css`** (bloc « HERO ACCUEIL ») : `.neykra-hero` (trame de points halftone renforcée, opacity 0.12), `.neykra-hero-lines--tl/tr/bl/br` (lignes de vitesse statiques des 4 coins vers le centre, mask dégradé, accent-1), `.neykra-hero-title` (halo -webkit-text-stroke couleur bg pour lisibilité du titre sur la tache d'encre, y compris univers clairs), `.neykra-highlight` (surlignage polygonal irrégulier accent-1), `.neykra-btn-hero` (coins coupés + micro-jitter via clip-path 16 points, ombre décalée dure accent, sans radius). Tout est piloté par les tokens `--neykra-*` de l'univers actif (aucune couleur en dur) → s'adapte automatiquement aux 5 univers.
- **Token `--neykra-on-accent`** ajouté aux 6 blocs (`:root` + 5 univers) : couleur de texte lisible sur fond accent-1 selon chaque univers (blanc sur VOID/ZEN, sombre sur NEON TOKYO/SAKURA/INFERNO).
- **Button `src/components/ui/Button.tsx`** : variante primary passe de `text-white` en dur à `text-[var(--neykra-on-accent)]` (contraste automatique par univers, ex. NEON TOKYO cyan → texte sombre).
- Build vérifié : ✓ compilé, TypeScript OK, 9/9 pages.

- **Server Actions `src/lib/posts/actions.ts`** : `createPost` (texte et/ou média uploadé vers le bucket Storage `posts`, détection MIME, max 5 Mo), `getFeedPosts` (50 posts les plus récents, join `profiles` + `otaku_status`, compteurs likes/commentaires, `has_liked`, **commentaires chargés en une requête avec join profiles et groupés par post**), `toggleLike`, `addComment`, `deletePost`, `deleteComment`. S'appuie sur la RLS existante (`post_visible_to_reader`, `*_delete_own`) pour la visibilité et les permissions.
- **Types `src/lib/posts/types.ts`** : `PostWithAuthor` (+ `comments?: CommentWithAuthor[]`), `CommentWithAuthor`, états de formulaires (`CreatePostFormState`, `LikeToggleState`, `CommentFormState`, `DeletePostFormState`, `DeleteCommentFormState`).
- **Migration `supabase/migrations/004b_posts_storage.sql` (À EXÉCUTER dans le SQL Editor Supabase)** : bucket public `posts` + 4 policies storage (lecture publique, écriture/modif/suppression réservées au dossier `<userId>/...`). Idempotente.
- **Composants `/feed`** : `create-post-form.tsx` (Card + textarea + upload média), `post-card.tsx` (Card : avatar, auteur lié au profil, badge rang otaku, date relative via `time-ago.ts`, média image/vidéo/gif, bouton like avec glow Button en état actif, commentaires), `comment-list.tsx`, `comment-form.tsx`, `logout-button.tsx`. Aucun HTML custom stylé à la main — Button/Card/Badge de `src/components/ui/` partout.
- **Corrections de bugs de build** : import `PostMediaType`/`CommentWithAuthor` manquants dans `actions.ts`, JSX invalide `href="/profile/" + x` → template literals (`post-card.tsx`, `comment-list.tsx`), `CommentList` reçoit désormais `comments` (au lieu d'un `postId` inutilisé), casts Supabase `as unknown as`, `form action` retour `Promise<void>`, message d'erreur `createPost` référençant le vrai fichier `004b_posts_storage.sql`, code mort `selfProfile/selfId` retiré.
- **Qualité** : `npm run build` ✓ (compilation + TypeScript OK).
- **À FAIRE côté humain** : exécuter `supabase/migrations/004b_posts_storage.sql` dans le SQL Editor Supabase (sinon l'upload de média affichera une erreur explicite).



## [13/09/2026] — Session 4 — Migration vers le système 5 univers (fondations)

- **Migration `supabase/migrations/003_theme_universes.sql` (À EXÉCUTER dans le SQL Editor Supabase)** : supprime l'ancien CHECK sur `profiles.theme_preference`, migre les données (`shonen` → `inferno`, `seinen` → `void`, `kawaii` → `sakura`, toute autre valeur → `void`), pose le défaut `theme_preference = 'void'`, ajoute le CHECK 5 univers (`void`/`neon_tokyo`/`sakura`/`inferno`/`zen`), supprime la colonne `profiles.color_mode` (+ ses CHECK résiduels). Idempotente (ré-exécutable sans erreur).
- **CSS `src/app/globals.css`** : pilotage par un seul attribut `data-universe="void|neon-tokyo|sakura|inferno|zen"` (`neon_tokyo` avec underscore aussi accepté), 5 blocs univers avec les codes couleur exacts de NEYKRA_SPEC.md §6 (`--neykra-bg`, `--neykra-accent-1`, `--neykra-accent-2`, `--neykra-text`), défaut `:root` = VOID, mapping Tailwind `@theme` étendu (`bg-neykra-bg`, `text-neykra-text`, …). Anciennes variables (`--background`, `--accent`, …) conservées en alias mappés par univers + mapping `data-theme="shonen|seinen|kawaii"` pour ne pas casser les composants existants.
- **Types `src/lib/theme/types.ts`** : nouveau type `Universe` + tableau `UNIVERSES` (5 entrées) + `LEGACY_THEME_TO_UNIVERSE` ; `Theme`/`ColorMode`/`THEMES`/`COLOR_MODES` conservés en `@deprecated` pour compatibilité.
- **ThemeProvider** : nouvel état `universe` + `updateUniverse`/`transitionUniverse`, attribut `data-universe` appliqué sur `<html>` (+ `data-theme`/`data-mode="dark"` en compat), normalisation des anciennes valeurs, lecture/écriture `theme_preference` uniquement (plus de `color_mode`). `updateColorMode` conservé en no-op déprécié.
- **Server Action `src/app/settings/actions.ts` (fondation, pas de visuel)** : `updateThemePreference` accepte les 5 univers + normalise les 3 anciens thèmes, n'écrit plus `color_mode` (2e param conservé optionnel/ignoré) ; `getProfilePreferences` ne lit plus que `theme_preference`.
- **Layout `src/app/layout.tsx`** : défaut `<html data-universe="void" data-theme="void" data-mode="dark">`.
- **Schéma `supabase/schema.sql`** : `profiles.theme_preference` défaut `'void'` + CHECK 5 univers, colonne `color_mode` retirée (aligné sur la migration 003 pour les fresh installs).
- **NON touchés (étape séparée après validation)** : `Button`/`Card`/`Badge`, `ThemeSelector.tsx`, `settings/page.tsx` et autres pages.
- **À FAIRE côté humain** : 1) exécuter `supabase/migrations/003_theme_universes.sql` dans le SQL Editor Supabase ; 2) lancer `npm run build` en local (l'outil d'exécution de commandes était en panne pendant cette session — build NON vérifié, à valider avant de merger) ; 3) `git add -A && git commit -m "feat: migration vers systeme 5 univers"` ; 4) étape suivante : migrer les composants visuels (`ThemeSelector` 5 univers, `Button`/`Card`/`Badge`, pages) + effets manga + réglages d'intensité dans `/settings`.

## [13/09/2026] — Session 3 — Phase 1bis : Fondation du système de design

- **Fonts** : remplacement de Geist par Anton (display, headers) + Inter (body) via `next/font/google` dans `src/app/layout.tsx`.
- **Attributs data-theme/data-mode** : ajout sur la balise `<html>` (valeurs par défaut : `data-theme="shonen"`, `data-mode="dark"`).
- **Variables CSS complètes** : système de 6 combinaisons (Shonen/Seinen/Kawaii × Dark/Light) défini dans `src/app/globals.css` avec couleurs, fonts, spacings, radii, shadows, focus styles, scrollbar, text selection.
- **Mapping Tailwind @theme** : correspondance des variables CSS vers les noms Tailwind (`bg-accent`, `text-background`, `border-border`, etc.) pour utilisation dans les classes `bg-*`, `text-*`, `border-*`, `ring-*`.
- **Framer Motion** : intégré (déjà installé en Phase 0), animations activées dans les composants UI (Button, Card, Badge) via `whileHover`, `whileTap`, `AnimatePresence`.
- **ThemeProvider** (`src/lib/theme/ThemeProvider.tsx`) : contexte React client qui lit/écrit les préférences dans `profiles.theme_preference` et `profiles.color_mode`, applique les attributs `data-theme`/`data-mode` sur `<html>`, gère l'état de chargement.
- **Types** (`src/lib/theme/types.ts`) : export de `Theme` (`shonen | seinen | kawaii`), `ColorMode` (`dark | light`), tableaux `THEMES` et `COLOR_MODES` pour l'affichage.
- **Composants UI** (`src/components/ui/`) :
  - `Button` : variant `primary | secondary | ghost | danger`, sizes `sm | md | lg`, état `loading` avec spinner, animations Framer Motion.
  - `Card` : padding `none | sm | md | lg`, option `hover` avec lift/shadow, component polymorphique (`div | article | section`).
  - `Badge` : variant `default | success | warning | error | info | accent | outline`, sizes `sm | md`, option `pulse` (animation SVG).
- **Page `/settings`** (`src/app/settings/page.tsx`) : sélecteur de thème (3 cartes avec aperçu visuel) + bascule sombre/clair (2 boutons), lecture des préférences depuis Supabase, sauvegarde via Server Action `updateThemePreference`, messages de confirmation.
- **Server Actions** (`src/app/settings/actions.ts`) : `updateThemePreference(theme, colorMode)` pour écrire dans `profiles`, `getProfilePreferences()` pour lire les préférences actuelles.
- **SupabaseProvider** (`src/lib/supabase/provider.tsx`) : contexte React client pour fournir l'instance Supabase browser au lieu de recréer le client à chaque composant.
- **Mise à jour des pages existantes** :
  - `/feed/page.tsx` : header avec lien vers `/settings`, styles CSS variables au lieu des classes zinc.
  - `/login/page.tsx` : styles CSS variables, message de confirmation avec couleurs succès.
  - `/signup/page.tsx` : styles CSS variables.
  - `/login/login-form.tsx` : inputs avec variables CSS, bouton via composant `Button`, messages d'erreur avec couleurs error.
  - `/signup/signup-form.tsx` : idem, bouton via `Button`.
  - `/feed/logout-button.tsx` : bouton avec variables CSS.
- **SCHEMA + MIGRATION** : colonne `color_mode` ('dark'/'light', default 'dark') ajoutée à `profiles` dans `supabase/schema.sql` + migration `supabase/migrations/002_add_color_mode.sql`.
- **SPEC** : NEYKRA_SPEC.md mis à jour — section 4 (schéma profiles) avec `color_mode`, section 6 complétée avec tableaux des palettes (Shonen/Seinen/Kawaii × Dark/Light), mode sombre/clair, et détails d'implémentation technique.
- **CHANGELOG** : cette entrée.

## [13/09/2026] — Session 2 — Phase 1 : Authentification
- **Dépendance** : `@supabase/ssr@0.12.7` ajouté (session cookies compatible SSR).
- **Clients Supabase** : `src/lib/supabase/client.ts` basculé sur `createBrowserClient` (SSR) + nouveau `src/lib/supabase/server.ts` (`createServerClient`, un par requête).
- **Inscription `/signup`** : formulaire (email, mot de passe, username, date de naissance **obligatoire**) + Server Action `signUp`. Validation serveur en français : champs manquants, email invalide, mot de passe < 6 caractères, username 3‑30 caractères (`[a-zA-Z0-9_]`), date de naissance invalide / future / avant 1900. Erreurs Supabase traduites (« email déjà utilisé », « email non confirmé », rate limit…).
- **Compte + profil** : `supabase.auth.signUp` avec `options.data` (username, birthdate) puis complétion de `profiles` (username, birthdate, is_minor, is_private). `is_minor` = moins de 18 ans à la date du jour (majorité au 18e anniversaire ; né le 29/02 → majorité le 28/02), calculé en TS (`src/lib/auth/age.ts`) **et** en SQL (`public.compute_is_minor`) — jamais de confiance en la valeur du client. `is_private = is_minor` (comptes mineurs privés par défaut).
- **Trigger enrichi** (`handle_new_user_auth` dans `supabase/schema.sql`) : lit `username` + `birthdate` depuis `raw_user_meta_data` et remplit `profiles` dès la création du compte auth → l'inscription fonctionne aussi avec **confirmation d'email activée** (pas de session immédiate).
- **Connexion `/login`** : `signInWithPassword`, redirection vers `/feed`, bannière de confirmation après le lien email (`/login?confirmed=1`).
- **Protection de route** : `src/proxy.ts` (ex-middleware Next.js 16) — refresh de session + `/feed` → `/login` (non connecté), `/login`/`/signup` → `/feed` (déjà connecté). La page `/feed` re-vérifie la session côté serveur.
- **Déconnexion** : bouton « Se déconnecter » dans l'en-tête de `/feed` (Server Action `signOut`).
- **Page d'accueil `/`** : landing minimal (Se connecter / Créer un compte) ; redirige vers `/feed` si déjà connecté.
- **Qualité** : `npm run lint` ✓, `npm run build` ✓ (compilation + TypeScript OK).

## [13/09/2026] — Session 1 — Phase 0 : Fondations
- **Réorganisation** : le dossier `Desktop\NEYKRA` (qui ne contenait que les 2 docs) est devenu la racine propre du projet — docs + code réunis au même endroit, rien ailleurs. *(Un dossier frère nommé "NEYKRA" est impossible : collision de nom.)*
- **Git** : dépôt initialisé (`git init -b main`), premier commit effectué — voir historique git.
- **Next.js 16.3.5** (App Router, TypeScript, Tailwind CSS v4, ESLint) scaffoldé à la racine, build validé (`npm run build` ✓ : compilation + TypeScript OK).
- **Supabase** : package `@supabase/supabase-js@2.116.0` installé, client créé dans `src/lib/supabase/client.ts`, variables dans `.env.local` (gitignoré) + gabarit `.env.example` (committé).
- **Schéma SQL** : `supabase/schema.sql` complet et conforme à NEYKRA_SPEC.md §4 — les 14 tables (`profiles`, `posts`, `likes`, `comments`, `friendships`, `conversations`, `conversation_participants`, `messages`, `notifications`, `blocks`, `quotes`, `otaku_status`, `quiz_questions`, `quiz_attempts`) + index + fonctions (`is_accepted_friend`, `is_blocked`, `post_visible_to_reader`) + trigger `on_auth_user_created` (profil auto à l'inscription).
- **RLS** : activation + politiques de base conformes à NEYKRA_SPEC.md §5 sur toutes les tables.
- **Framer Motion 13.2.0** installé (pas encore utilisé, réservé aux phases suivantes).
- **README.md** réécrit pour NEYKRA.

## Ce qui reste à faire
- **Exécuter `supabase/schema.sql` (au minimum la section 5 : `compute_is_minor` + trigger `handle_new_user_auth`) dans le SQL Editor de Supabase** — indispensable pour que l'inscription remplisse bien le profil (username, birthdate, is_minor, is_private), y compris avec confirmation d'email activée.
- Créer le dépôt **GitHub** distant (authentification requise) et `git push`.
- Vérifier de bout en bout inscription / connexion / déconnexion sur le projet Supabase branché (RLS).
- Phase 2 — Profil & fil d'actualité (voir NEYKRA_SPEC.md §9).

## Bugs / points d'attention connus
- Aucun bug bloquant. Terminal Windows capricieux pour les sorties longues / builds (scaffold/install) — résolu par logs redirigés + build en arrière-plan.
- **Attention** : si `schema.sql` a déjà été exécuté AVANT cette session, le trigger `handle_new_user_auth` doit être rejoué — sinon, avec confirmation d'email activée, le profil restera incomplet (username `user_<id>`, birthdate NULL).
- Username déjà pris + confirmation d'email activée → message d'erreur générique côté inscription (le conflit unique remonte pendant le signUp, pas lors de la mise à jour du profil).
- `quiz_answers` verrouillée (aucune policy) — les clients passeront par `check_quiz_answer` en Phase 6.